{
  "name": "kinohub-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @kinohub/schemas"
  },
  "dependencies": {
    "date-fns-tz": "^3.2.0"
  }
}
